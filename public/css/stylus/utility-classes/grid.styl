/**
 * Class utilitaires pour les grid
 *
 * .flex => display: flex
 * 
 * Les width sont sont composer de prefix "w-" suivi
 * de la valeur, "1/3", un tiers.
 * Les valeurs vont jusqu'à 12, (1/12, 2/12, ...) et
 * commence à 2, (1/2, 2/2)
 * 
 * Ex:
 * .w-1/3 => width: 33.3334%
 * .w-2/3 => width: 66.6667%
 * .w-3/3 => width: 100%
 * .w-1/4, .w-2/4, .w-4/5, .w-5/8, w-3/11 (se sont que quelque exemple ...)
 * 
 */


.flex
    display: flex
    flex-wrap: wrap

$prefix = 'w-'

for $i in $widthPercentage
    for $y in 1..$i
        $class = '.' + $prefix + $y + '\/' + $i
        {$class}
            width: (100 / $i) * $y + '%'
        
        